#!/bin/bash

case "$1" in
    (aggie)
        git config user.name "Richard Snider";
        git config user.email "A02298443";;
    (lhm)
        git config user.name "Richard Snider";
        git config user.email "lighthousemaniac@gmail.com";;
    (*)
        echo "bad identity";
        exit 1;;
esac
